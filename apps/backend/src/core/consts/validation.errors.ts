import { CommonErrorCodeEnum } from '@shared-types/dist';

export const COMMON_ERRORS = {
  VALIDATION_ERROR: {
    code: CommonErrorCodeEnum.VALIDATION_ERROR,
    field: 'feild',
    message: 'data in field is invalid',
  },
};
